#=========================================================================
# Basic Application Configuration:
# Environment: local, staging, production
# Application encryption key (generated via php artisan key:generate)
# Show detailed error messages (true for development, false for production)
# Main application URL
# API base URL for frontend consumption
# Trusted proxy configuration for load balancers
#========================================================================
APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
APP_API_URL=http://api-laravel.jaxonville.com
APP_TRUSTED_PROXIES=*

#========================================================================
# Localization Settings:
# Default application language
# Fallback language if translation missing
# Locale for generating fake data in tests/seeders
#========================================================================
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

#========================================================================
# Maintenance Mode Configuration:
# Storage driver for maintenance mode (file or database)
#========================================================================
APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

#========================================================================
# API Rate Limiting:
# Enable/disable API request throttling
#========================================================================
API_THROTTLE=true

#========================================================================
# Asana Configuration:
# Asana PAT of user we're acting on behalf of
# Asana Workspace GID
#========================================================================
ASANA_ACCESS_TOKEN=TOKEN
ASANA_WORKSPACE_GID=WORKSPACE_GID
#========================================================================
# Laravel Pulse Configuration (Performance Monitoring):
# Subdomain for Pulse dashboard (null = use main domain)
# URL path for accessing Pulse dashboard
# Enable/disable Pulse monitoring
#========================================================================
PULSE_DOMAIN=null
PULSE_PATH=pulse
PULSE_ENABLED=true

#========================================================================
# Development Server Configuration:
# Number of workers for built-in PHP server
PHP_CLI_SERVER_WORKERS=4
#========================================================================

#========================================================================
# Password Hashing Configuration:
# Bcrypt hashing rounds
#========================================================================
BCRYPT_ROUNDS=12

#========================================================================
# Logging Configuration:
# Primary logging channel
# Stack channels to log to simultaneously
# Separate channel for deprecation warnings
# Include stack traces for deprecation warnings
# Minimum log level (debug, info, notice, warning, error, critical, alert, emergency)
# Days to keep daily log files
#========================================================================
LOG_CHANNEL=stack
LOG_STACK=single,web,api,slack
LOG_DEPRECATIONS_CHANNEL=null
LOG_DEPRECATIONS_TRACE=false
LOG_LEVEL=debug
LOG_DAILY_DAYS=14

#========================================================================
# Slack Logging Integration:
# Slack webhook URL for error notifications
# Username shown in Slack notifications
# Emoji for Slack log messages
#========================================================================
LOG_SLACK_WEBHOOK_URL=
LOG_SLACK_USERNAME="Laravel Log"
LOG_SLACK_EMOJI=":boom:"

#========================================================================
# Database Configuration:
# Database driver (mysql, mariadb, pgsql, sqlite, sqlsrv)
# Database server host
# Database server port
# Database name
# Database username
# Database password
#========================================================================
DB_CONNECTION=mariadb
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravelsite
DB_USERNAME=larauser
DB_PASSWORD=password

#========================================================================
# Session Configuration:
# Session storage driver (file, cookie, database, redis, memcached, array)
# Session lifetime in minutes
# Encrypt session data
# Session cookie path
# Session cookie domain
#========================================================================
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

#========================================================================
# Broadcasting Configuration:
# Broadcasting driver (log, redis, pusher, reverb, null)
#========================================================================
BROADCAST_CONNECTION=log

#========================================================================
# File Storage Configuration:
# Default filesystem disk (local, public, s3)
#========================================================================
FILESYSTEM_DISK=local

#========================================================================
# Queue Configuration:
# Queue driver (sync, database, redis, sqs, null)
#========================================================================
QUEUE_CONNECTION=database

#========================================================================
# Cache Configuration:
# Cache driver (array, database, file, memcached, redis)
# Prefix for cache keys
#========================================================================
CACHE_STORE=database
# CACHE_PREFIX=

#========================================================================
# Memcached Configuration:
# Memcached server host
#========================================================================
MEMCACHED_HOST=127.0.0.1

#========================================================================
# Redis Configuration:
# Redis client (predis or phpredis)
# Redis server host
# Redis server password
# Redis server port
#========================================================================
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

#========================================================================
# Mail Configuration:
# Mail driver (smtp, sendmail, mailgun, ses, postmark, log, array)
# Mail encryption scheme
# SMTP server host
# SMTP username
# SMTP password
# Default "from" email address
# Default "from" name
#========================================================================
MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

#========================================================================
# AWS Configuration:
# AWS access key for S3, SES, etc.
# AWS secret access key
# AWS region
# S3 bucket name
# Use path-style S3 endpoints
#========================================================================
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

#========================================================================
# Frontend Build Configuration (Vite):
# App name available to frontend build process
#========================================================================
VITE_APP_NAME="${APP_NAME}"
